# Service URLs
AI_REPLY_GENERATOR_URL=http://localhost:8002
SMS_GATEWAY_URL=http://localhost:8000

# Gmail SMTP Configuration
GMAIL_SMTP_SERVER=smtp.gmail.com
GMAIL_SMTP_PORT=465
EMAIL_ADDRESS=your-email@gmail.com
EMAIL_PASSWORD=your-app-specific-password

# SMS Configuration
YOUR_PHONE_NUMBER=+1234567890

# Database Configuration
DB_HOST=192.168.1.228
DB_PORT=5432
DB_NAME=email_auto_reply
DB_USER=readwrite
DB_PASSWORD=your-database-password

# Workflow Configuration
POLL_INTERVAL=120
USER_RESPONSE_TIMEOUT=86400
MAX_RETRY_ATTEMPTS=3
MAX_EDIT_ITERATIONS=10
MAX_EMAILS_PER_POLL=5  # Safety limit: max emails to process per poll cycle (set to 1 for testing, 0 to disable)
SMS_FORMAT=condensed
ORCHESTRATOR_BASE_URL=https://sms.jackan.xyz  # Public URL for draft hosting (used in SMS notifications)

# Server Configuration
HOST=0.0.0.0
PORT=8003
